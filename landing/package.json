{
  "name": "tab-minder-landing",
  "version": "1.0.0",
  "description": "Landing page for Tab Minder Chrome Extension",
  "scripts": {
    "start": "serve -s .",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
